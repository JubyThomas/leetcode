def strStr(self, haystack: str, needle: str) -> int:
        try:
            res = haystack.index(needle)
            return res
        except:
            return -1
        s=[]
        s.de
        
        
x= strStr("a","a")
print(x)